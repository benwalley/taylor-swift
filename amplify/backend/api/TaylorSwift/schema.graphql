type Album @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
}

type Song @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  album: String
  lyrics: String
  year: String
  singer: String
  writer: String
  notes: String
  other: String
}
